import { styled } from "styled-components";

export const Footer = styled.div`
    width: 100%;
    position: fixed;
    bottom: 0;
    padding: 20px;
    display: flex;
    justify-content: end;
    Button {
        background-color: transparent;
    }
`;
