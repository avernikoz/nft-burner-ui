import styled from "styled-components";

export const Footer = styled.div`
    Button {
        background-color: transparent;
        position: fixed;
        margin: 1rem;
        bottom: 0;
        right: 0;
    }
`;
